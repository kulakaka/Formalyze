{
  "dependencies": {
    "chart.js": "^4.4.5",
    "firebase": "^10.14.1",
    "react-chartjs-2": "^5.2.0",
    "react-firebase-hooks": "^5.1.1"
  },
  "scripts": {
    "start:client": "cd client && npm install && npm start",
    "start:server": "cd server && npm install && npm start",
    "start": "npm run start:client & npm run dev:server"
  }
}
